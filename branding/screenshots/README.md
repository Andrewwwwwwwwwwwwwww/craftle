# Screenshots

| File | Shot |
|---|---|
| `01-board.png` | A fresh daily board — empty grid, output slot, ingredient palette |
| `02-solved.png` | A solved daily — history grids down both flanks, coloured palette, result revealed (taken with **high contrast mode on**, so the "correct" tiles read blue rather than the default green) |
| `03-practice.png` | The same board in `/craftle random` practice mode |

These are committed so the GitHub README can link them by relative path, and so Modrinth
can use the `raw.githubusercontent.com` URLs:

```
https://raw.githubusercontent.com/Andrewwwwwwwwwwwwwww/craftle/master/branding/screenshots/01-board.png
https://raw.githubusercontent.com/Andrewwwwwwwwwwwwwww/craftle/master/branding/screenshots/02-solved.png
```

**CurseForge serves images from its own CDN.** Upload each one on the project's *Images*
tab, or paste it straight into the description editor, which uploads it for you — then
swap the placeholders in `../DESCRIPTION.md` for the `media.forgecdn.net` links it returns.
